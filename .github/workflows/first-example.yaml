name: my-first-workflow

on:
    push

jobs:
    first-job:
        runs-on: ubuntu:latest
        steps:
        - name: Welcome message
          run: echo "My first github action job"
        - name: List files
          run: ls
        - name: Read File
          run: cat README.md


